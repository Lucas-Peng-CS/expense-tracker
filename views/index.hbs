<div class="container">
  <div class="row">
    <div class="col-6 mx-auto">
      <form action="">
        <div class="form-group">
          <select class="form-control mt-5" id="exampleFormControlSelect1">
            <option>類別</option>
            <option value="家居物業">家居物業</option>
            <option value="交通出行">交通出行</option>
            <option value="休閒娛樂">休閒娛樂</option>
            <option value="餐飲食品">餐飲食品</option>
            <option value="其他">其他</option>
          </select>
        </div>
      </form>
    </div>
  </div>
  <div class="row justify-content-center">
    <div class="col-1 align-self-center">總金額:</div>
    <div class="col-5 totalAmount" style="color:rgb(250, 197, 137)">
      <h1>{{this.totalAmount}}</h1>
    </div>
  </div>

  {{#each records}}
  <div class="container">
  {{!-- {{#each categories}}
  {{this.icon}}
  {{/each}} --}}
  {{this.category}}
  {{this.name}}{{this.date}}{{this.amount}}
  </div>
  {{/each}}
</div>